let name = ['Anns', 'Andrew', 'Tyler', 'Michelle'];

function nameNum(arr) {
    let newArr = [];
    for (let i = 0; i < arr.length; i++) {
       newArr.push.(arr[i].length);
       console.log(arr);
    }
    newArr();
}

function length(arr) {
return arr.map(e => e.length)
}
console.log(langth(name));

// length(arr);